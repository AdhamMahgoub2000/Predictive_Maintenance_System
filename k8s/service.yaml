apiVersion: v1
kind: Service
metadata:
  name: predictive-maintenance-api
  labels:
    app: predictive-maintenance-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: predictive-maintenance-api


